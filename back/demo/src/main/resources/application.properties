
#spring.datasource.url = jdbc:mysql://localhost:3306/crud?useSSL=false&serverTimezone=UTC&useLegacyDateTimeCode=false
spring.datasource.url=jdbc:postgresql://localhost:5432/GestionDeCursos
# server.port = 8090

# nombre de usuario y contrase?a
spring.datasource.username=postgres
spring.datasource.password=admin

# mostrar sentencias SQL en la consola
spring.jpa.show-sql = true

# actualizar base de datos y crear entidades
spring.jpa.hibernate.ddl-auto =  update
spring.jpa.properties.hibernate.format_sql=true

# hibernate genera SQL optimizado
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect

# scurity , jwt.secret esta será la variable para la firma de seguridad
jwt.secret = secret 
jwt.expiration = 36000

spring.mvc.pathmatch.matching-strategy=ant-path-matcher

# mvn spring-boot:run comando para iniciar tu app de spring